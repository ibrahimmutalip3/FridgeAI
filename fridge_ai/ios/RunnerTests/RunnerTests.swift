import Flutter
import UIKit
import XCTest

class RunnerTests: XCTestCase {

  func testExample() {
    // A minimal smoke test so the RunnerTests target has something to run.
    XCTAssertTrue(true)
  }

}
