# This file is generated automatically by `flutter pub get` / the Flutter
# tool (and by GitHub Actions' subosito/flutter-action) — it points Gradle
# at the local Flutter SDK checkout and is NOT meant to be committed with
# machine-specific values in a normal project.
#
# It is included here only so the repository has a valid placeholder; CI
# and any local `flutter` invocation will overwrite it automatically.
flutter.sdk=/opt/flutter
