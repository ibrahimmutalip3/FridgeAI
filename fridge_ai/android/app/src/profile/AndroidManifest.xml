<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for profile builds to allow the
         Flutter tool to communicate with the app (e.g. DevTools/profiling). -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
